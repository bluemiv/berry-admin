import React from 'react';

const Header = () => {
  return (
    <header className="sticky top-0 h-header flex items-center px-md bg-background/60 dark:bg-background-dark/60 backdrop-blur-sm">
      header
    </header>
  );
};

export default Header;
